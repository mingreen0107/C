#include <stdio.h>
#include <string.h>

int main() {

	char arr[100] = { 0 };
	int len;
	int i;

	scanf_s("%s", arr);

	len = strlen(arr);

	for (i = 0; i < len / 2; i++)
	{
		arr[]
	}




	return 0;
}